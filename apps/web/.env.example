# URL absolue de l'API en production (build statique Render).
# En développement, laisser VIDE : le proxy Vite redirige /api → http://127.0.0.1:8000.
# En production : VITE_API_URL=https://samacommerce-api.onrender.com/api
VITE_API_URL=
